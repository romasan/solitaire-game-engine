'use strict';

export default function(data) {// {actionData, eventData, eventName}

	console.log('stepsAround', data);

	// data.actionData.run // make step(one) | run action
	// data.actionData.dispatch
	
	// find around by relations

	// let clt = data.eventData.deck.getTopCArd(),
	// 	top = data.eventData.deck.getTopCArd(),
	// 	crt = data.eventData.deck.getTopCArd(),
	
	// 	lft = data.eventData.deck.getTopCArd(),
	// 	rgt = data.eventData.deck.getTopCArd(),

	// 	clb = data.eventData.deck.getTopCArd(),
	// 	btm = data.eventData.deck.getTopCArd(),
	// 	crb = data.eventData.deck.getTopCArd();
};