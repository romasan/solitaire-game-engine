'use strict'

export default (deck, cardIndex) => {
	// let cardId      = deck.cards[cardIndex].id;
	// let cardElement = share.get('domElement:' + cardId);
	// cardElement.addClass('mark');
}
